# Not built -- this is a version pin, read by scripts/scan_verified_secrets.sh
# and .github/workflows/secret-scan.yml, so Dependabot has one real manifest
# to bump instead of a bash variable it can't see.
FROM trufflesecurity/trufflehog:3.97.4
