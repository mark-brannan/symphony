# Not built -- this is a version pin, read by scripts/gitleaks_precommit.sh
# and .github/workflows/secret-scan.yml, so Dependabot has one real manifest
# to bump instead of a bash variable it can't see. See RUNBOOK.md's removed
# "Upgrading the scanners" section (git history) for why this exists.
FROM zricethezav/gitleaks:v8.30.1
