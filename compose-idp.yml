services:
  dex:
    image: ghcr.io/dexidp/dex:latest
    container_name: dex
    profiles: ["tls"]
    restart: unless-stopped
    volumes:
      - ./dex/config.yaml:/etc/dex/config.docker.yaml:ro
    networks:
      - symphony-net
